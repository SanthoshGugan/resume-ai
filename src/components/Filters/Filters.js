import React from "react";
import { Container } from "react-bootstrap";

const Filters = ({}) => {

    return (
        <Container>
            Filters
        </Container>
    );
};

export default Filters;